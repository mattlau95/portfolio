# UX Audit Report — Matthew C. Lau Portfolio

> **Date:** 2026-06-20 · **Reviewed:** `index.html`, `projects/edison-dental.html` (source) · `http://localhost:4321` (Part A) · **Tools run:** Lighthouse 12, axe-core 4.11.4, pa11y, manual code review

---

## Executive summary

The homepage is in excellent shape post-audit-sweep: Performance 99, Accessibility 100, Best Practices 100, with LCP 1.8s and Speed Index 1.4s — the `display=optional` fix from this morning eliminated the 6.6s Speed Index reading entirely. The Edison Dental case study page has one P0 (empty table header, WCAG 1.3.1 violation flagged by both axe and pa11y) and a LCP regression to 2.8s caused by its render-blocking Google Fonts stylesheet, which was never migrated to the async pattern used by `index.html`. **1 blocker, 2 important, 2 polish.**

**Snapshot**

- Checklist items passing: **34 / 37**
- Core Web Vitals (homepage): LCP **1.8s** ✓ · TBT **0ms** ✓ · CLS **0** ✓
- Core Web Vitals (Edison Dental): LCP **2.8s** ⚠ · TBT **0ms** ✓ · CLS **0.046** ✓
- Speed Index (homepage): **1.4s** ✓ (was 6.6s — `display=optional` fix confirmed effective)
- Lighthouse (homepage): Performance **99** · Accessibility **100** · Best Practices **100**
- Lighthouse (Edison Dental): Performance **91** · Accessibility **100** · Best Practices **100**
- Accessibility — axe-core 4.11.4: **0 violations** (homepage) · **1 violation** (Edison Dental)
- pa11y: **0 issues** (homepage) · **1 error** (Edison Dental)

---

## Severity tiers

| Tier | Meaning | When to fix |
|------|---------|-------------|
| 🔴 **P0 — Blocker** | WCAG 2.2 AA violation (legal exposure) or loses user data | Before you ship |
| 🟡 **P1 — Important** | Real friction or a clear standards miss, but the app still works | This week |
| 🟢 **P2 — Polish** | Refinement that raises quality — nothing is actually wrong | When you have room |

**Effort:** ⚡ *Quick Win* (~30 min) · 🔨 *Deep Work* (cross-cutting)

---

## 🔴 P0 — Blockers

| # | Finding | Checklist ref | Evidence | Fix | Effort |
|---|---------|---------------|----------|-----|--------|
| 1 | Lighthouse comparison table in Edison Dental has an empty `<th></th>` corner cell and all `<th>` elements lack `scope` attributes — WCAG 1.3.1 violation | §7 — Accessible tables | axe-core `empty-table-header` + pa11y WCAG2AA.1.3.1.H43 · `edison-dental.html:67–71` | Change `<th></th>` → `<th scope="col" aria-label="Version">` (or the text label of your choice, e.g. "Site"); add `scope="col"` to the four column headers in `<thead>`; add `scope="row"` to the two row headers in `<tbody>` | ⚡ |

---

## 🟡 P1 — Important

| # | Finding | Checklist ref | Evidence | Fix | Effort |
|---|---------|---------------|----------|-----|--------|
| 1 | Edison Dental LCP 2.8s — over the 2.5s threshold. Root cause: Google Fonts CSS loaded as a render-blocking `<link rel="stylesheet">` (794ms render-blocking time per Lighthouse). `index.html` was fixed to use the async preload+onload pattern; the case study page was never updated to match | §9 — LCP < 2.5s | Lighthouse `largest-contentful-paint`: 2.8s; `render-blocking-insight`: fonts.googleapis.com blocks 794ms · `projects/edison-dental.html:12` | Replace the blocking `<link href="..." rel="stylesheet" fetchpriority="high">` with the same async preload pattern used in `index.html`: `<link rel="preload" as="style" href="...&display=optional" onload="this.onload=null;this.rel='stylesheet'">` + `<noscript>` fallback. Also change `display=swap` → `display=optional` to match the homepage and prevent FOUT | ⚡ |
| 2 | Edison Dental footer Lighthouse badges are stale — show "Performance 99 / LCP 1.4s" but current measurements are 91 / 2.8s | §1 — System status | `edison-dental.html:82–86`. The 1.4s LCP reading was from the June 2026 Phase 4 audit before today's measurement diverged | Update badges after fixing P1.1 above (fixing font loading should improve LCP significantly and Performance will follow) | ⚡ |

---

## 🟢 P2 — Polish

| # | Finding | Checklist ref | Evidence | Fix | Effort |
|---|---------|---------------|----------|-----|--------|
| 1 | Project thumbnails are 800×600px source but displayed at ~350×260px (standard cards) and ~300×170px (featured card) — Lighthouse estimates 80 KiB savings with properly sized images | §9 — Performance | Lighthouse `image-delivery-insight`: 5 images flagged, largest waste is `edison-dental-thm.webp` (30 KiB wasted, 1000×563 served at 522×391 displayed). All others similarly oversized | Add `srcset` pointing to 400×300 versions alongside the 800×600 originals, or resize source images to ~400px wide — the display size never exceeds 650px even on large screens. WebP already in use, just needs size variants | 🔨 |
| 2 | Homepage footer Performance badge shows 94 — today's Lighthouse reads 99 | §1 — System status | `index.html:312`. The 94 was the previous audit's reading; the `display=optional` and `fetchpriority` changes this morning improved the score | Update to 99 after confirming production score holds | ⚡ |

---

## Priority action plan

1. **[P0 ⚡]** Table scope — add `scope="col"` to all `<thead> <th>` and `scope="row"` to `<tbody> <th>` in `edison-dental.html:67–71`; give the corner `<th>` an `aria-label` or visible text
2. **[P1 ⚡]** Edison Dental font loading — switch `projects/edison-dental.html:12` from blocking `<link rel="stylesheet">` to the async preload+onload pattern with `display=optional`, matching `index.html`
3. **[P1 ⚡]** Update Edison Dental footer badges after fixing P1.1 (re-run Lighthouse, get real numbers)
4. **[P2 ⚡]** Update homepage Performance badge from 94 → 99 in `index.html:312`
5. **[P2 🔨]** Responsive thumbnail images — add `srcset` with ~400px variants for all project card `<img>` tags

---

## What passed

- **Homepage CWV all green** — LCP 1.8s, TBT 0ms, CLS 0, Speed Index 1.4s (was 6.6s; `display=optional` fix confirmed working)
- **Homepage Lighthouse 99/100/100** — best scores to date across all three categories
- **axe-core + pa11y: zero violations on homepage**
- **Touch targets** — `padding-inline: var(--space-3)` on both `nav a` and `.socials a` clears WCAG 2.2 SC 2.5.8 ✓
- **External link indicators** — all 13 `target="_blank"` links now show ↗ via CSS `::after` ✓
- **`prefers-reduced-motion` respected** throughout — spotlight, transitions, scroll-behavior all conditioned ✓
- **Scroll-spy initial state** — "Projects" correctly active on fresh page load ✓
- **Semantic HTML, focus states, alt text, color contrast, skip link** — all clean ✓
- **CLS 0 (homepage)** — explicit `width`/`height` on all images prevents layout shift ✓
- **`fetchpriority="high"` on primary photo** — LCP element hinted correctly ✓
- **`loading="lazy"` on secondary photo** — hidden-by-default image no longer fetched eagerly ✓
